import React, { Component } from 'react';

const SlideTwo= (props) => {

  let background = {
    backgroundImage: 'url(calypso.jpg)',
    backgroundSize: 'cover',
    backgroundPosition: 'center'
  }

  return <div className="slide"></div>
}

export default SlideTwo;
