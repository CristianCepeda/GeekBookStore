import React, { Component } from 'react';

const SlideOne= (props) => {

  let background = {
    backgroundImage: 'url(the scarlet letter.jpg)',
    backgroundSize: 'cover',
    backgroundPosition: 'center'
  }

  return <div className="slide"></div>
}

export default SlideOne;
